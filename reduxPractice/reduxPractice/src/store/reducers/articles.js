const initialState = {
  items: [],
  loading: false,
  error: null
};

const articleReduser = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default articleReduser;
